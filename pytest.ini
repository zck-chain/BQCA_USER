[pytest]
# scratch/ holds local benchmark/profiling scripts whose functions happen to be
# named test_* but take non-fixture arguments. Exclude them from collection so
# `pytest` collects only the real test suite under tests/.
norecursedirs = scratch .git .venv venv build dist
